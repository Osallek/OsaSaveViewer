export default function BaseTransformer(debug: any): void;
export default class BaseTransformer {
    constructor(debug: any);
    debug: any;
    validate(logicalFamily: any, strongValidate: any): void;
}
