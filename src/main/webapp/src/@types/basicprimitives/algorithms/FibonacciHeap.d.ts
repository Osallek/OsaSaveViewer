/**
* Heap result object
* @class HeapResult
* @property {string} key Key
* @property {number} priority Priority
* @property {Object} item Context object
*/
export function HeapResult(node: any): void;
export class HeapResult {
    /**
    * Heap result object
    * @class HeapResult
    * @property {string} key Key
    * @property {number} priority Priority
    * @property {Object} item Context object
    */
    constructor(node: any);
    key: any;
    priority: any;
    item: any;
}
/**
 * Creates Fibonacci Heap structure
 * @class FibonacciHeap
 *
 * @param {boolean} isMaximum Is maximum heap
 * @returns {FibonacciHeap} Returns new FibonacciHeap object
 */
export default function FibonacciHeap(isMaximum: boolean): FibonacciHeap;
export default class FibonacciHeap {
    /**
     * Creates Fibonacci Heap structure
     * @class FibonacciHeap
     *
     * @param {boolean} isMaximum Is maximum heap
     * @returns {FibonacciHeap} Returns new FibonacciHeap object
     */
    constructor(isMaximum: boolean);
}
